% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segMINT_db.R
\name{create_database}
\alias{create_database}
\title{Create DuckDB database from simulation results}
\usage{
create_database(
  dir,
  file_name,
  table_name,
  data = NULL,
  data_dir = NULL,
  N = NULL,
  randomize = FALSE,
  demo = TRUE
)
}
\arguments{
\item{dir}{Character string. Directory where the database will be created}

\item{file_name}{Character string. Name of the database file (e.g., "db_test.duckdb")}

\item{table_name}{Character string. Name of the table to create/update}

\item{data}{Data frame. Optional. If provided, writes this data to the database. 
If NULL, you must provide data_dir and N parameters}

\item{data_dir}{Character string. Directory containing RDS files (required if data is NULL)}

\item{N}{Either "all" or an integer. Number of files to process (required if data is NULL)}

\item{randomize}{Logical. Whether to randomize file processing order (default: FALSE)}

\item{demo}{Logical. Whether to run demo queries after creating database (default: TRUE)}
}
\value{
Invisibly returns TRUE on success
}
\description{
Creates or updates a DuckDB database with simulation results
}
\examples{
\dontrun{
# Option 1: Process files directly to database
create_database("/path/to/db", "my_db.duckdb", "results_table", 
                data_dir = "/path/to/data", N = "all")

# Option 2: Use pre-processed data
results <- get_results("/path/to/data", "all")
create_database("/path/to/db", "my_db.duckdb", "results_table", data = results)
}
}
